name: Deploy React App to S3

on:
  push:
    branches:
      - main

permissions:
  id-token: write
  contents: read

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18

      # ✅ FIXED FOLDER NAME HERE
      - name: Install Dependencies
        working-directory: ./singlepage-website
        run: npm install

      # ✅ FIXED FOLDER NAME HERE
      - name: Build Project
        working-directory: ./singlepage-website
        run: npm run build

      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: arn:aws:iam::654485376127:role/github-actions-role
          aws-region: ap-south-1

      # React build output path inside folder
      - name: Upload to S3
        run: |
          aws s3 sync ./singlepage-website/build s3://bucket-for-npm-static-website --delete
